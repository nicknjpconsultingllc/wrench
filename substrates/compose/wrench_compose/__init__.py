"""WRENCH compose substrate: a docker-compose job pipeline as the disrupted system.

Pure logic (signing, arming, picking, allowlist, positions) lives here and is
shared by the in-container services and the host-side episode runner.
"""
